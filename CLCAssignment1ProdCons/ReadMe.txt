CLC Assignment 1: Producers & Consumers

This program demonstrates a solution to the Producer-Consumer problem using threads in C. The producer thread generates integers and places them in a shared buffer. 
The consumer thread retrieves those values and prints them. Synchronization is handled using a shared flag to ensure proper operation.
